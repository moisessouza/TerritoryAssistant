package ged.mediaplayerremote.domain.model;

/**
 * An enum representing playback state in {@link PlaybackStatus}
 */
public enum PlaybackState {
    NO_FILE_SELECTED,
    STOPPED,
    PAUSED,
    PLAYING

}
